/// <reference types="vite/client" />

/**
 * Vite ambient types for the marketing app. The `vite/client` reference above
 * declares `*.css` and `*.module.css` module shapes so the global stylesheet
 * import and CSS Module imports are typed under `tsc --noEmit`.
 */
